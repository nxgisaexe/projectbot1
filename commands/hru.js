module.exports = {
    name: 'hru',
    description: "a basic hru command.",
    run(message, args){
        message.channel.send(`meow! i'm ok, thanks for asking!`);
    }
}
// this a single command file.